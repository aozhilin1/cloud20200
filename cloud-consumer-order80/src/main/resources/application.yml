server:
  port: 80

spring:
  application:
    name: cloud-order-service
  # zipkin/sleuth链路跟踪

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka